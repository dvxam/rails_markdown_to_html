<%= form_for @post do |f| %>
  <%= f.label :content %>
  <%= f.text_area :content %><br />

  <%= f.submit %>
<% end %>

<% @posts.each do |post| %>
	<p><%= raw Redcarpet.new(post.content).to_html %></p>
<% end %>
